---
// Small fact pill used in the home hero (icon + text).
import { Icon } from 'astro-icon/components';

interface Props {
  icon: string;
}

const { icon } = Astro.props;
---

<span class="chip">
  <Icon name={icon} class="text-accent h-3.5 w-3.5" aria-hidden="true" />
  <slot />
</span>
