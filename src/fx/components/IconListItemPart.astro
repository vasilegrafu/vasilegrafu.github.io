---
// One "What drives me" entry: gold icon, bold lead-in, body text in the slot.
import { Icon } from 'astro-icon/components';

interface Props {
  icon: string;
  title: string;
}

const { icon, title } = Astro.props;
---

<li class="flex gap-3">
  <Icon name={icon} class="mt-1 h-5 w-5 shrink-0 text-label" aria-hidden="true" />
  <span><strong class="text-ink">{title}</strong> <slot /></span>
</li>
